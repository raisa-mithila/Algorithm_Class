#include<bits/stdc++.h>
using namespace std;
int main()
{
    stack<int>st;
    int n;
    cin>>n;
    int num;
    for(int i=0; i<n; i++)
    {
        cin>>num;
        st.push(num);

    }
    st.pop();
    /*st.push(30);
    st.push(40);
    st.push(50);
    st.push(60);
    st.pop();
    st.pop();*/

    while(!st.empty())
    {
        cout<<st.top()<<endl;
        st.pop();
    }

    return 0;
}

